import { ThumbnailToImageSrcPipe } from './thumbnail-to-image-src.pipe';

describe('ThumbnailToImageSrcPipe', () => {
  it('create an instance', () => {
    const pipe = new ThumbnailToImageSrcPipe();
    expect(pipe).toBeTruthy();
  });
});
